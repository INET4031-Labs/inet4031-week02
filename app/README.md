# Application Source Code

The professor will provide the incident tracking application source code. Place it in this directory.

Expected files:
- `Dockerfile` - container image definition for the Flask application
- `app.py` - the Flask API application
- `requirements.txt` - Python package dependencies

Do not modify the provided application code. Your responsibility is to configure how it runs via Docker Compose.
